---
title: Ansible / AWX
description: Connect Castrel to Ansible AWX or Ansible Automation Platform for querying and triggering automation jobs.
navigation:
  icon: i-simple-icons-ansible
---

## Overview

- **Connector name**: `ansible`
- **Supported API version**: AWX REST API v2
- **Supported server version**: AWX 21.0+ / Ansible Automation Platform 2.0+
- **Official docs**: [AWX API Reference](https://ansible.readthedocs.io/projects/awx/en/latest/rest_api/api_ref.html)

## Configuration

To use Ansible tools, create a Connector with the following fields:

| Field | Required | Description |
|-------|----------|-------------|
| `base_url` | Yes | AWX/AAP server URL (e.g., `https://awx.example.com`) |
| `auth_type` | Yes | Authentication type: `basic` (username/password) or `bearer` (Access Token) |
| `username` | No | Username (required when `auth_type=basic`) |
| `password` | No | Password (required when `auth_type=basic`) |
| `access_token` | No | Personal Access Token (required when `auth_type=bearer`) |
| `verify_ssl` | No | Verify SSL certificates (default: `true`) |

## Available Tools

This connector provides a comprehensive set of tools for automation job management.

- **Job queries**: List and query job execution history, status, and console output
- **Template management**: Retrieve Job Template and Workflow Template lists
- **Job triggering**: Launch automation jobs via Job Templates with parameters
- **Inventory queries**: Query host inventories and host group information
- **Credential management**: List available credential types and configurations

## Useful Links

- [AWX REST API Reference](https://ansible.readthedocs.io/projects/awx/en/latest/rest_api/api_ref.html)
- [Ansible Automation Platform Docs](https://access.redhat.com/documentation/en-us/red_hat_ansible_automation_platform)
- [AWX Personal Access Tokens](https://docs.ansible.com/automation-controller/latest/html/userguide/applications_auth.html)
