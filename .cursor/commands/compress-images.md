请执行项目内图片压缩脚本：`node scripts/compress-images.mjs`。

执行规则：
1. 如果我在命令后附带参数，则原样拼接到脚本命令后执行。
2. 如果我没有附带参数，则使用默认参数执行：`--dir=public/images --max-width=2000 --quality=80 --min-size-kb=800`。
3. 执行完成后，输出：
   - 实际执行的完整命令
   - 压缩统计（处理数量、总大小变化、压缩比例）
   - 受影响文件列表（从命令输出中提取）
4. 如果执行失败，直接输出错误原因和可重试命令，不要改动其他文件。
